'use client';
import Image from 'next/image';
import { Header } from '@/components/shengya/Header';
import { Footer } from '@/components/shengya/Footer';
import { useDoctors } from '@/components/shengya/useDoctors';
export default function DoctorsPage() { const doctors = useDoctors(); return <><Header /><main className="sy-inner-page"><div className="sy-page-hero sy-page-hero-doctors"><div className="sy-container sy-page-hero-inner"><div><p className="sy-kicker">OUR DOCTORS</p><h1>专业团队，<br />悉心相伴</h1></div><p>我们重视医学依据，也重视您的感受。每一次诊疗，都从充分沟通和专业判断开始。</p></div></div><section className="sy-section sy-doctor-list"><div className="sy-container"><div className="sy-list-intro"><p>MEDICAL TEAM</p><span>认识为您提供咨询与诊疗服务的专业团队</span></div><div className="sy-doctor-grid sy-doctor-grid-all">{doctors.map((doctor, index) => <article className="sy-doctor-card" key={doctor.name}><div><Image src={doctor.image} alt={doctor.name} fill sizes="(max-width: 700px) 50vw, 25vw" /><span className="sy-doctor-index">{String(index + 1).padStart(2, '0')}</span></div><h3>{doctor.name}</h3><p>{doctor.title}</p><small>{doctor.focus}</small></article>)}</div><div className="sy-doctor-cta"><p>不确定该选择哪一位医生？</p><a className="sy-btn sy-btn-dark" href="/contact">告诉我们您的需求</a></div></div></section></main><Footer /></>; }
