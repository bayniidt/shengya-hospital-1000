'use client';
import Image from 'next/image';
import { useEffect, useState } from 'react';
import { environments } from './data';
export function Gallery() { const [active, setActive] = useState<number | null>(null); useEffect(() => { const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') setActive(null); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, []); return <><div className="sy-gallery">{environments.map((src, i) => <button type="button" className={i === 0 ? 'sy-gallery-main' : ''} key={src} onClick={() => setActive(i)} aria-label={`查看医院环境 ${i + 1}`}><Image src={src} alt="圣娅医院环境" fill sizes="(max-width: 700px) 100vw, 50vw" /><span>VIEW {String(i + 1).padStart(2, '0')}</span></button>)}</div>{active !== null && <div className="sy-lightbox" role="dialog" aria-modal="true" onClick={() => setActive(null)}><button className="sy-lightbox-close" onClick={() => setActive(null)} aria-label="关闭">×</button><div onClick={(e) => e.stopPropagation()}><Image src={environments[active]} alt="圣娅医院环境大图" width={1400} height={900} /></div><p>圣娅医院环境 · {String(active + 1).padStart(2, '0')} / {String(environments.length).padStart(2, '0')}</p></div>}</>; }
