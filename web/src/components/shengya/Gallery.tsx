'use client';
import Image from 'next/image';
import { useEffect, useState } from 'react';
import { environments } from './data';
import { useLanguage } from './LanguageProvider';
export function Gallery() { const [active, setActive] = useState<number | null>(null); const { text } = useLanguage(); useEffect(() => { const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') setActive(null); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, []); return <><div className="weui-cells sy-gallery" data-stagger>{environments.map((src, i) => <button type="button" className={`weui-cell weui-cell_active ${i === 0 ? 'sy-gallery-main' : ''}`} key={src} onClick={() => setActive(i)} aria-label={text(`查看医院环境 ${i + 1}`, `View hospital environment ${i + 1}`)}><Image src={src} alt={text('圣娅医院环境', 'Saintia hospital environment')} fill sizes="(max-width: 700px) 100vw, 50vw" /><span>VIEW {String(i + 1).padStart(2, '0')}</span></button>)}</div>{active !== null && <div className="weui-gallery sy-lightbox" role="dialog" aria-modal="true" aria-label={text(`圣娅医院环境图片 ${active + 1}`, `Saintia hospital environment image ${active + 1}`)} onClick={() => setActive(null)}><button className="weui-icon-btn weui-icon-btn_close sy-lightbox-close" onClick={() => setActive(null)} aria-label={text('关闭', 'Close')} /><div className="weui-gallery__img" onClick={(e) => e.stopPropagation()}><Image src={environments[active]} alt={text('圣娅医院环境大图', 'Saintia hospital environment')} width={1400} height={900} /></div><p className="weui-gallery__opr">{text('圣娅医院环境', 'SAINTIA ENVIRONMENT')} · {String(active + 1).padStart(2, '0')} / {String(environments.length).padStart(2, '0')}</p></div>}</>; }
