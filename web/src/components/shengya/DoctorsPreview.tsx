'use client';
import Image from 'next/image';
import Link from 'next/link';
import { useEffect, useState } from 'react';
import { useDoctors } from './useDoctors';
export function DoctorsPreview() { const doctors = useDoctors(); const [selected, setSelected] = useState<typeof doctors[number] | null>(null); useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.key === 'Escape') setSelected(null); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, []); return <section className="sy-section sy-doctors"><div className="sy-container"><div className="sy-section-heading sy-doctors-heading"><div><p className="sy-kicker">OUR DOCTORS</p><h2>专业团队，悉心相伴</h2></div><p>每一位医生都以严谨、审美与沟通，参与您的专属方案。</p></div><div className="sy-doctor-grid">{doctors.slice(0, 4).map((doctor) => <button type="button" className="sy-doctor-card" key={doctor.name} onClick={() => setSelected(doctor)}><div><Image src={doctor.image} alt={doctor.name} fill sizes="(max-width: 700px) 50vw, 25vw" /></div><h3>{doctor.name}</h3><p>{doctor.title}</p><small>查看医生资料 →</small></button>)}</div><Link className="sy-link" href="/doctors">查看完整团队 →</Link></div>{selected && <div className="sy-doctor-modal" role="dialog" aria-modal="true" aria-label={`${selected.name}医生资料`} onClick={() => setSelected(null)}><div onClick={(e) => e.stopPropagation()}><button className="sy-modal-close" onClick={() => setSelected(null)} aria-label="关闭">×</button><div className="sy-modal-photo"><Image src={selected.image} alt={selected.name} fill sizes="320px" /></div><div className="sy-modal-copy"><p className="sy-kicker">SAINTIA MEDICAL CENTER</p><h2>{selected.name}</h2><strong>{selected.title}</strong><p>{selected.focus}</p><a className="sy-btn sy-btn-dark" href="/contact">预约咨询</a></div></div></div>}</section>; }
