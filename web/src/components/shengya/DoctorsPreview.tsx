'use client';
import Image from 'next/image';
import Link from 'next/link';
import { useEffect, useState } from 'react';
import { useDoctors } from './useDoctors';
import { useLanguage } from './LanguageProvider';
export function DoctorsPreview() { const doctors = useDoctors(); const { text } = useLanguage(); const [selected, setSelected] = useState<typeof doctors[number] | null>(null); useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.key === 'Escape') setSelected(null); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, []); return <section className="sy-section sy-doctors" data-reveal><div className="sy-container"><div className="sy-section-heading sy-doctors-heading"><div><p className="sy-kicker">OUR DOCTORS</p><h2>{text('专业团队，悉心相伴', 'Expert care, every step of the way')}</h2></div><p>{text('每一位医生都以严谨、审美与沟通，参与您的专属方案。', 'Every doctor brings clinical care, aesthetic judgment and open communication to your personal plan.')}</p></div><div className="sy-doctor-grid" data-stagger>{doctors.slice(0, 4).map((doctor) => <button type="button" className="weui-cell weui-cell_access sy-doctor-card" key={doctor.image} onClick={() => setSelected(doctor)}><div><Image src={doctor.image} alt={doctor.name} fill sizes="(max-width: 700px) 50vw, 25vw" /></div><h3>{doctor.name}</h3><p>{doctor.title}</p><small>{text('查看医生资料', 'View Profile')} →</small></button>)}</div><Link className="sy-link" href="/doctors">{text('查看完整团队', 'Meet the Full Team')} →</Link></div>{selected && <><div className="weui-mask sy-modal-mask" onClick={() => setSelected(null)} /><div className="weui-half-screen-dialog weui-half-screen-dialog_show sy-doctor-modal" role="dialog" aria-modal="true" aria-label={text(`${selected.name}医生资料`, `${selected.name} profile`)}><div className="weui-half-screen-dialog__hd"><div className="weui-half-screen-dialog__hd__side"><button className="weui-icon-btn weui-icon-btn_close" onClick={() => setSelected(null)} aria-label={text('关闭', 'Close')} /></div><div className="weui-half-screen-dialog__hd__main"><strong className="weui-half-screen-dialog__title">{text('医生资料', 'Doctor profile')}</strong></div></div><div className="weui-half-screen-dialog__bd sy-doctor-sheet"><div className="sy-modal-photo"><Image src={selected.image} alt={selected.name} fill sizes="320px" /></div><div className="sy-modal-copy"><p className="sy-kicker">SAINTIA MEDICAL CENTER</p><h2>{selected.name}</h2><strong>{selected.title}</strong><p>{selected.focus}</p><Link className="weui-btn weui-btn_primary sy-btn sy-btn-dark" href="/contact">{text('预约咨询', 'Book a Consultation')}</Link></div></div></div></>}</section>; }
