'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useEffect, useState } from 'react';
export function Header() { const pathname = usePathname(); const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const onScroll = () => setScrolled(window.scrollY > 30); onScroll(); window.addEventListener('scroll', onScroll, { passive: true }); return () => window.removeEventListener('scroll', onScroll); }, []); const active = (path: string) => pathname === path; return <header className={`sy-header ${scrolled ? 'scrolled' : ''} ${pathname !== '/' ? 'sy-header-solid' : ''}`}><div className="sy-container sy-header-inner"><Link href="/" className="sy-logo" aria-label="圣娅医疗中心首页"><span>圣娅</span><small>SAINTIA MEDICAL CENTER</small></Link><nav className={open ? 'is-open' : ''} aria-label="主导航"><Link className={active('/') ? 'active' : ''} aria-current={active('/') ? 'page' : undefined} href="/" onClick={() => setOpen(false)}>首页</Link><div className="sy-nav-group"><button type="button">医疗服务 <b>⌄</b></button><div className="sy-nav-dropdown"><Link href="/#services" onClick={() => setOpen(false)}>服务总览</Link><Link href="/#services" onClick={() => setOpen(false)}>医美整形</Link><Link href="/#services" onClick={() => setOpen(false)}>精密体检</Link><Link href="/#services" onClick={() => setOpen(false)}>再生医疗</Link></div></div><Link className={active('/doctors') ? 'active' : ''} aria-current={active('/doctors') ? 'page' : undefined} href="/doctors" onClick={() => setOpen(false)}>医生介绍</Link><Link className={active('/contact') ? 'active' : ''} aria-current={active('/contact') ? 'page' : undefined} href="/contact" onClick={() => setOpen(false)}>联系我们</Link><Link className="sy-nav-cta" href="/contact" onClick={() => setOpen(false)}>预约咨询</Link></nav><button className={`sy-menu ${open ? 'is-active' : ''}`} onClick={() => setOpen(!open)} aria-label={open ? '关闭菜单' : '打开菜单'} aria-expanded={open}><i /><i /><i /></button></div></header>; }
