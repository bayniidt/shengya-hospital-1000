'use client';
import { FormEvent, useState } from 'react';
export function ContactForm() { const [status, setStatus] = useState(''); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setStatus('提交中…'); const form = e.currentTarget; const data = Object.fromEntries(new FormData(form)); try { const res = await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); if (!res.ok) throw new Error(); form.reset(); setStatus('已收到您的信息，我们会尽快与您联系。'); } catch { setStatus('提交失败，请直接拨打咨询电话。'); } } return <form className="sy-form" onSubmit={submit}><label>您的称呼<input required name="name" placeholder="请输入姓名" /></label><label>联系电话<input required name="phone" type="tel" placeholder="请输入手机号" /></label><label>咨询内容<textarea required name="message" rows={4} placeholder="请简单描述您的需求" /></label><label className="sy-check"><input required type="checkbox" /> 我已阅读并同意隐私说明</label><button className="sy-btn sy-btn-dark" type="submit">提交咨询</button>{status && <p className="sy-form-status" role="status">{status}</p>}</form>; }
