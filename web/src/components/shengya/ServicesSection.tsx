'use client';
import Image from 'next/image'; import { useState } from 'react';
const services = [{ label: '医美整形', title: '自然、精细的美学方案', copy: '以专业评估为起点，尊重每个人独特的面部与身体表达。', image: '/ribenpeizhen/1715131816328486.jpg' }, { label: '精密体检', title: '用科学了解身体', copy: '从检查到解读，建立更适合您的健康管理路径。', image: '/ribenpeizhen/1715131568213192.jpg' }, { label: '再生医疗', title: '探索生命的修复力', copy: '关注前沿医学进展，提供严谨、透明的咨询服务。', image: '/ribenpeizhen/1715131691832026.jpg' }, { label: '免疫疗法', title: '让健康管理更长期', copy: '以规范流程和个体化沟通，陪伴您做出理性选择。', image: '/ribenpeizhen/1715131725691509.jpg' }];
export function ServicesSection() { const [active, setActive] = useState(0); const service = services[active]; return <section id="services" className="sy-services"><div className="sy-container"><div className="sy-services-head"><div><p className="sy-kicker">OUR SERVICES</p><h2>专业服务，因人而异</h2></div><p>好的医疗体验，应该清晰、被理解，也值得被认真对待。</p></div><div className="sy-service-tabs" role="tablist" aria-label="医疗服务分类">{services.map((item, index) => <button type="button" role="tab" id={`service-tab-${index}`} aria-controls={`service-panel-${index}`} aria-selected={active === index} key={item.label} className={active === index ? 'active' : ''} onClick={() => setActive(index)}>{String(index + 1).padStart(2, '0')} <span>{item.label}</span></button>)}</div><div className="sy-service-feature" role="tabpanel" id={`service-panel-${active}`} aria-labelledby={`service-tab-${active}`} key={service.label}><div className="sy-service-copy"><p className="sy-kicker">0{active + 1} / 04</p><h3>{service.title}</h3><p>{service.copy}</p><a href="/contact" className="sy-link">咨询服务详情 →</a></div><div className="sy-service-image"><Image src={service.image} alt={service.label} fill sizes="(max-width: 700px) 100vw, 55vw" /></div></div></div></section>; }
