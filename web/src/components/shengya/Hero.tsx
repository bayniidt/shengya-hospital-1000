'use client';
import { useSiteContent } from './useSiteContent';
export function Hero() { const content = useSiteContent(); return <section className="sy-hero reveal"><div className="sy-hero-bg" style={{ backgroundImage: `linear-gradient(90deg,rgba(38,13,58,.88),rgba(67,20,86,.28)),url('${content.heroImage}')` }} /><div className="sy-container sy-hero-content"><p className="sy-kicker">SAINTIA MEDICAL CENTER · SHANGHAI</p><h1>{content.tagline.split('相遇')[0]}<br /><em>相遇{content.tagline.split('相遇')[1] || '于圣娅'}</em></h1><p className="sy-hero-copy">以医疗专业为根基，以真诚服务为桥梁，陪伴您遇见更好的自己。</p><div><a className="sy-btn sy-btn-light" href="/contact">预约咨询</a><a className="sy-btn sy-btn-ghost" href="#about">认识圣娅</a></div></div><span className="sy-scroll-cue">SCROLL <i /></span></section>; }
