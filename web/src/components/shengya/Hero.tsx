'use client';
import { useSiteContent } from './useSiteContent';
import Link from 'next/link';
import { useLanguage } from './LanguageProvider';
export function Hero() { const content = useSiteContent(); const { language, text } = useLanguage(); const taglineParts = language === 'zh' ? content.tagline.split('相遇') : [content.tagline, '']; return <section className="sy-hero reveal"><div className="sy-hero-bg" data-parallax style={{ backgroundImage: `url('${content.heroImage}')` }} /><div className="sy-container sy-hero-content"><p className="sy-kicker" data-hero-motion>SAINTIA MEDICAL CENTER · SHANGHAI</p><h1 data-hero-motion><span>{taglineParts[0]}{language === 'zh' ? '，' : ''}</span>{language === 'zh' && <em>相遇{taglineParts[1] || '于圣娅'}</em>}</h1><p className="sy-hero-copy" data-hero-motion>{text('以医疗专业为根基，以真诚服务为桥梁', 'Grounded in medical expertise and connected by sincere care.')}</p><div className="sy-hero-actions" data-hero-motion><Link className="weui-btn weui-btn_primary sy-btn sy-btn-light" href="/contact">{text('预约咨询', 'Book a Consultation')}</Link><a className="weui-btn weui-btn_default sy-btn sy-btn-ghost" href="#about">{text('认识圣娅', 'Discover Saintia')}</a></div><div className="sy-hero-meta" aria-hidden="true">01 / MEDICAL CARE<br />SHANGHAI · 2026</div></div></section>; }
